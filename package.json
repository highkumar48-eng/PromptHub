{
  "name": "prompthub",
  "version": "1.0.0",
  "description": "AI Prompt Discovery Platform",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "dev": "vite --host 127.0.0.1",
    "templates": "node scripts/templates.js",
    "build": "npm run templates && vite build",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "ejs": "^3.1.10",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "drizzle-kit": "^0.31.10",
    "vite": "^8.2.2"
  }
}
